{
  "name": "tianji-hecan-prototype",
  "version": "0.2.0",
  "private": true,
  "description": "I Ching, Vedic astrology and western astrology divination prototype with MiniMax AI report generation.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check app.js && node --check api/generate-reading.js && node --check server.js && node --check sw.js"
  },
  "engines": {
    "node": ">=20"
  }
}
